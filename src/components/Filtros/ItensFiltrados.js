/* import React from 'react'
import Filtros from './Filtros'

export default class ItensFiltrados extends React.Component {
 
    render() {
    return (<div>
          <h3> produtos</h3>
          {this.props.imagens.map((product) => {
              if(product.value <= {this.props.minimoFiltro}  || )
            return (
             
            );
          })}
        </div>
    )
  }
} */